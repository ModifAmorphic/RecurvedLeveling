scn rcvConvertSecondsToTicks

; inputs
float seconds
; output
int ticks
Begin _Function { seconds } ; Converts seconds to ticks, rounding up if needed
	if (Recurved.RecurvedControllerInterval > 0)
		set ticks to Ceil (seconds / Recurved.RecurvedControllerInterval)
	endif
	SetFunctionValue ticks
End